> 注意，`pathSegments` 接受測試輸出目錄的路徑片段，例如 `testInfo.outputPath('relative', 'path', 'to', 'output')`。
> 然而，這個路徑必須保留在每個測試的 [testInfo.outputDir](/api/class-testinfo.mdx#test-info-output-dir) 目錄內（即 `test-results/a-test-title`），否則會拋出錯誤。