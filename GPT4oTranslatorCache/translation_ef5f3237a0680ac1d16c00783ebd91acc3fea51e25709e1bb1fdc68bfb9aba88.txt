- [Dict]<a aria-hidden="true" tabIndex="-1" class="list-anchor-link" id="request-sizes-return"/><a href="#request-sizes-return" class="list-anchor">#</a>
  - `requestBodySize` [int]
    
    請求主體（POST 資料有效負載）的大小，以位元組為單位。如果沒有主體，則設為 0。
  - `requestHeadersSize` [int]
    
    從 HTTP 請求訊息開始到主體之前（包括）雙 CRLF 的總位元組數。
  - `responseBodySize` [int]
    
    接收到的回應主體（編碼）的大小，以位元組為單位。
  - `responseHeadersSize` [int]
    
    從 HTTP 回應訊息開始到主體之前（包括）雙 CRLF 的總位元組數。