1. 找到與 `selector` 匹配的元素。如果沒有，則等待直到匹配的元素附加到 DOM。
1. 確保匹配的元素是複選框或單選輸入。如果不是，這個方法會拋出錯誤。如果元素已經未選中，這個方法會立即返回。
1. 等待匹配元素的[可操作性](../actionability.mdx)檢查，除非設置了 `force` 選項。如果在檢查期間元素被分離，整個操作將重試。
1. 如果需要，將元素滾動到視圖中。
1. 使用 [Page.mouse()](/api/class-page.mdx#page-mouse) 點擊元素的中心。
1. 確保元素現在未選中。如果沒有，這個方法會拋出錯誤。