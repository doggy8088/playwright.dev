- [Promise]&lt;[null] | [物件]&gt;<a aria-hidden="true" tabIndex="-1" class="list-anchor-link" id="element-handle-bounding-box-return"/><a href="#element-handle-bounding-box-return" class="list-anchor">#</a>
  - `x` [number]
    
    元素的 x 座標（以像素為單位）。
  - `y` [number]
    
    元素的 y 座標（以像素為單位）。
  - `width` [number]
    
    元素的寬度（以像素為單位）。
  - `height` [number]
    
    元素的高度（以像素為單位）。