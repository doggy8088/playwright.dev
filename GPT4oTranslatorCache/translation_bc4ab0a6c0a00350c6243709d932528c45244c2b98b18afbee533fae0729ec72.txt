- 新的 CLI 選項 `--fail-on-flaky-tests`，在任何不穩定的測試時將退出代碼設置為 `1`。請注意，預設情況下，當所有失敗的測試在重試後恢復時，測試執行器會以代碼 `0` 退出。使用此選項，在這種情況下測試執行將失敗。
- 新的環境變量 `PLAYWRIGHT_FORCE_TTY` 控制內建 `list`、`line` 和 `dot` 報告器是否假設為即時終端。例如，當您的 CI 環境無法很好地處理 ANSI 控制序列時，這可能會很有用。或者，如果您計劃後處理輸出並處理控制序列，即使沒有即時終端，也可以啟用 tty 行為。