- [Promise]&lt;[null] | [物件]&gt;<a aria-hidden="true" tabIndex="-1" class="list-anchor-link" id="locator-bounding-box-return"/><a href="#locator-bounding-box-return" class="list-anchor">#</a>
  - `x` [number]
    
    元素的 x 座標（像素）。
  - `y` [number]
    
    元素的 y 座標（像素）。
  - `width` [number]
    
    元素的寬度（像素）。
  - `height` [number]
    
    元素的高度（像素）。